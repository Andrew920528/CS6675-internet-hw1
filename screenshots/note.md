stat.png - The result of running `nutch/bin/nutch readdb crawl/crawldb -stats`. In the python script, it is executed every 5 second to get a snapshot of crawling process.

keyword.png - The text file we got after running `nutch/bin/nutch readlinkdb crawl/linkdb/ -dump link_content`. The anchors of the urls are used as the keyword for analysis. The text file is later parsed to a csv file.
